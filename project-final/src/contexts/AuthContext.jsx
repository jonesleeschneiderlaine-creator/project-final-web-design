// src/contexts/AuthContext.jsx
// ─── Objet contexte uniquement (séparé pour fast refresh) ────────────────────

import { createContext } from 'react';

export const AuthContext = createContext(null);